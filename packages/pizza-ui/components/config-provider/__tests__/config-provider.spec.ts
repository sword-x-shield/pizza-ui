// import { mount } from '@vue/test-utils';
// import ConfigProvider from '../src/config-provider.vue';

describe('config-provider.vue', () => {
  test('should render config-provider', () => {
    // Arrange
    // const wrapper = mount(ConfigProvider);

    // Act

    // Assert
  });
});
